[[language]]
name = "rust"
scope = "source.rust"
file-types = ["rs"]

[language.auto-pairs]
'<' = '>'

[[language]]
name = "bash"
scope = "source.bash"
file-types = [
  ".config",
  ".aliases",
  "ignore",
  ".env",
  ".bashrc",
  ".bash_profile",
  ".profile",
]

# if uncomment toml syntax does not work
# [[language]]
# name = "toml"
# scope = "source.toml"
# file-types = [".ini"]

[[language]]
name = "markdown"
formatter = { command = "prettier", args = ["--parser", "markdown"] }
